---
description: Build one offline, clickable HTML prototype of a single slice of a requirement (design-system-styled, mocked logic, verified by driving it).
---

Invoke the **interactive-poc** skill and follow `METHOD.md`.

Context for this run (optional — the skill will ask if missing):

$ARGUMENTS

Start at step 1: read the requirement source, propose one slice + a screen list, and confirm with
me before building. Then decide the actor (my call), and proceed through `poc-kit`
add-ds → init → build → verify → handoff.
